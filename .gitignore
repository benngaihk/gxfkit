# Rust
/target
**/*.rs.bk

# Corpus: track the download script + manifest, not the (large) data
/corpus/raw/

# Benchmark outputs (regenerated by benchmark/run.sh)
/benchmark/results/
/benchmark/vendor/

# AGAT scratch
*.agat.log
*.agat.stdout

# OS / editor
Thumbs.db
.DS_Store
*.swp

# Local runtime / scheduler state
.claude/
