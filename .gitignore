# Rust
/target
**/*.rs.bk

# Corpus: track the download script + manifest, not the (large) data
/corpus/raw/

# Benchmark outputs (regenerated by benchmark/run.sh)
/benchmark/results/
/benchmark/vendor/
/fuzz/target/

# AGAT scratch
*.agat.log
*.agat.stdout

# OS / editor
Thumbs.db
.DS_Store
*.swp

# proptest failure seeds (none yet); keep if any appear
