# gxfkit-core

Core parser, model, and conversion library for `gxfkit`.

This crate is primarily published so the `gxfkit` binary crate can be installed
from Cargo. The public API is still alpha and may change before 1.0.

Repository: https://github.com/benngaihk/gxfkit
